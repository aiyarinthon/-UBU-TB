import { Patient, DailyLog, InvestigationForm, ContactPerson, ContactFollowUp } from '../types';

// Initial empty database so the user can start entering fresh, real records
export const INITIAL_PATIENTS: Patient[] = [];
export const INITIAL_INVESTIGATIONS: InvestigationForm[] = [];
export const INITIAL_CONTACTS: ContactPerson[] = [];
export const INITIAL_FOLLOWUPS: ContactFollowUp[] = [];
export const INITIAL_DAILY_LOGS: DailyLog[] = [];
